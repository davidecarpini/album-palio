/** @format */

import { AppRegistry } from "react-native";
import WrapperPersitor from "./WrapperPersistor";
import { name as appName } from "./app.json";

AppRegistry.registerComponent(appName, () => WrapperPersitor);
